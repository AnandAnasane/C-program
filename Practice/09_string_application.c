// String applications
// strlen (str) -- to count the string
// strcpy(str1, str2) -- to copy from str2(source) to str1 (destination). not swap.
// strcat (str1, str2) -- to merg (concatenate) two strings   
// strcmp (str) -- to compaire two string return 0 if same; return 1 if different.
//  format specifier -- S - used for string.
# include <stdio.h>
# include <string.h>

int main()
{
    char str1[] = "mahabharat";
    char str2[] = "Ramayana";

    
    
     
   
    // // To check the length of string....
    // printf("The length of string str1 is %d", strlen(str1));

    // to copy string from ramayana to Mahabharat== output is Ramayana.
    // printf("To copy string from str2 to str1 : %s", strcpy(str1, str2));

     // to merg the two string
    // printf("To concate i.e merging ==== %s", strcat(str1, str2));


    // To compaire to string if they are same or not
    // return 0 if they are same.
    //  return 1 if they are different

    
    // char str1 [] = "abc";
    // char str2 [] = "abc";
    //     int isSame = strcmp(str1, str2);

    //     if (isSame == 0)
    //     {
    //         printf("Same....!!!");
    //     }
    //     else
    //     {
    //         printf("Different...!!!");
    //     }

    return 0;


}