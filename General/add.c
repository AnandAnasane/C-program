#include <stdio.h>

int main()
{
    int a, b, sum;
    printf("Enter the first number :");
    scanf ("\n%d", &a);
    printf("Enter the second number:");
    scanf("\n%d", &b);
    sum = a*b;

    printf("The addition of a and b is %d", sum);

}